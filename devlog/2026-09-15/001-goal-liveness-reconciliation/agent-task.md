修复 Ralph V3 Native Goal liveness / stalled reconciliation：增加周期性 thread/goal/get，区分 Goal status、transport liveness 与 meaningful progress；持久化 goal_liveness.json 与 goal_stall.json；active 且无 workspace、agent message、artifact 进展超过可配置阈值时，显式 pause 并确认 paused，返回 GOAL_STALLED 技术结果；app-server 断开时只重连同一 thread，不创建 replacement Goal；close() 不隐式 pause，提供 controlled shutdown pause helper；补齐 terminal notification 丢失、transport exit、token-only growth、stall 和 controlled shutdown 回归测试。不要修改 Payroll、Finder、GUI selector 或 autonomous-obligation 架构。
