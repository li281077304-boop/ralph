Implement Ralph V3 autonomous obligation loop in this repository.

Scope: add formal CHIEF_RECOVERY phase and durable technical recovery for Worker blocked/technical failures; add canonical on-disk obligations with statuses RUNNABLE, TECHNICAL_OPEN, HUMAN_BLOCKED, PASS; add durable Human Backlog so one human-blocked obligation never stops independent runnable work; add deterministic scheduler and global WAITING_FOR_HUMAN only when no runnable/technical work remains, DONE only when all obligations pass; add stable failure signatures and fresh-context recovery; add SIGINT/SIGTERM controlled pause-and-confirm for active Native Goals. Preserve existing V3 state-machine meaning, Git/Machine Gate/checkpoint safety, Host Codex Chief, GUI fallback, and durable devlog contract. Do not touch Payroll/Dashboard or add unrelated architecture.

Chief Recovery must use fresh read-only Host Codex Sol/high and strict CHIEF_RECOVERY_JSON with RETRY_WORKER, RUN_MACHINE_GATE, HUMAN_BLOCK. Technical errors (tests/build/missing commands/UI/loopback/sandbox/stall) must never become human blocks; only the five allowed human categories are valid. Persist worker block evidence, recovery handoff/decision, failure signatures, Human Backlog, and resume context. Never start a replacement Goal or bypass invariant failures.

Add deterministic state-machine, fault-injection, and 8-obligation autonomous scenario tests: technical retry, gate route, human backlog while other work continues, WAITING_FOR_HUMAN, resolved backlog to DONE, malformed/mismatched protocol, repeated failure signature, crash/restart/liveness/disconnect, and signal shutdown. Keep existing tests passing. Then run full tests, typecheck, build, prettier, and diff-check. Do not claim real Payroll UAT until it is actually run; after code gates pass, run the existing real Payroll workload and report TOTAL/PASS/TECHNICAL_OPEN/HUMAN_BLOCKED/FAILED, Chief Recovery count, recovery success count, and human interrupt count. Commit as: feat: add autonomous obligation-driven V3 loop. Push feature/ralph-v3-autonomous-obligation-loop.
