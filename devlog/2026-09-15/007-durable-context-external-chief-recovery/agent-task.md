Implement the durable semantic discussion-context hard gate and restore historically verified External Chief Playwright capabilities on top of the current Ralph model-economics baseline. First ensure every formal Worker, direct codex-task, and Chief SELECT/REVIEW/RECOVERY handoff persists context.md, exact agent-task.md, task-hash.txt, and metadata.json, reads them back, validates substantive required sections (USER OBSERVATION, CONFIRMED FACT, TECHNICAL ASSESSMENT, REJECTED ASSUMPTIONS, DECISION, UNKNOWN / OPEN RISKS), and fails closed with zero Agent invocation when context is missing, template-only, malformed, or hash/metadata mismatched. Keep an explicit minimal/test escape hatch recorded in metadata and do not elevate Agent permissions because Devlog is enabled. Make this task’s own entry pass the new gate.

Before changing the GUI bridge, create a durable three-way capability diff for commits ebdaa9c08b9d98aacf8b5a09d11952b7fc580567, 139985ee557866dee20bbbc8fb956554d2a23bb2, and current HEAD. Restore only demonstrably lost capabilities: previous resolved conversation reuse, configured/existing-tab scan, autonomous ChatGPT acquisition, writable composer probing and selectors, bounded reload recovery/classification, durable resolved/submission handoff state, existing complete/partial reply recovery, and duplicate submission protection. Preserve current visible+enabled+editable candidate selection, existing-reply probe, timeout/incomplete distinction, strict protocol, External-first/Host-Sol fallback, and model economics. Add preflight/attempt classification without counting missing configuration or unavailable attach as a real attempt.

Add a tracked Verified Capability Registry, semantic-context tests, External Chief regression tests, and real Playwright evidence. Run focused tests, full suite, typecheck, build, modified-file Prettier, and git diff --check. Do not modify Payroll or Dashboard, do not change V3 state-machine semantics, and do not run long Payroll UAT. Record result/decision and the six real bridge/External Chief outcomes when the environment permits; otherwise preserve diagnostics and classify the exact boundary.
