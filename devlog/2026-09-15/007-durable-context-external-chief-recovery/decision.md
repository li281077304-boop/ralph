# Decision

## Confirmed conclusions

1. The previous Devlog contract proved only that files existed and hashes matched; it did not prove that discussion context contained real semantics.
2. Formal tasks now use one programmatic semantic gate before invocation and fail closed without a substantive context.
3. The historical External Chief transport was real and was split from the current line during branch evolution; file presence alone was not evidence that its capabilities survived.
4. The regression was silent capability loss during branch divergence, not proof that Playwright is inherently uncontrollable.
5. Historical conversation acquisition/reuse, tab scanning, bounded ChatGPT opening, full composer selectors, diagnostics, bounded reload, and durable submission identity have been restored while retaining current visible/enabled/editable selection and existing-reply recovery.

## Rejected assumptions

- The user’s original observation that the page was stuck selecting a file was not upgraded to a confirmed root cause.
- The 2026-09-13 “3+3 smooth rounds” memory is retained as USER_OBSERVED, not as an exact SHA-bound fact.
- The most recent Payroll External Chief counter of 1/0/1 was not treated as a real attempt because GUI configuration/preflight was absent.

## Open risks

- Real Playwright UAT is blocked by a confirmed Cloudflare human-verification challenge in the current session; this is not a code-level selector failure.
- Top-level CLI signal handling still does not automatically call the controlled Goal pause helper.
- A real authenticated session is needed to establish the requested six live roundtrips and Host Sol=0 evidence.

## Live Playwright result

- Bridge attempts: `0` real submissions, `0` successes, `0` transport failures. Preflight/acquisition reached the existing Chrome tab, but the page was stopped at a confirmed Cloudflare human-verification challenge before any message was sent.
- Ralph External Chief attempts: `0`; Host Sol calls during this live probe: `0`.
- Requested 3 bridge + 3 Ralph External Chief sequence: `NOT-YET-VERIFIED`, stopped at the explicitly allowed `HUMAN_VERIFICATION_REQUIRED` boundary.
