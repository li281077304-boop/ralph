# Decision

CONFIRMED CONCLUSIONS

- Default Worker execution uses finite stage turns; Native Goal remains opt-in compatibility behavior.
- Chief routing supports External-first with automatic Host Sol fallback while preserving strict existing decision parsing and durable state transitions.
- Isolated worktree creation is explicit and never merges or pushes production branches.

REJECTED ASSUMPTIONS

- A host Codex launcher stall is not evidence that the implementation is complete or that Payroll UAT passed.
- A fallback transport does not relax protocol binding or acceptance checks.

ARCHITECTURE DECISIONS

- Keep liveness, Goal, Machine Gate, and V3 state protocols intact; add only narrow routing and isolation seams.
- Persist fallback telemetry and keep all Chief invocations fresh.

KNOWN OPEN RISKS

- Real Payroll autonomous validation remains pending.
- No automatic merge is performed for isolated worktrees.

NEXT RECOMMENDED ACTION
Run the full Ralph gate, then perform an identity-checked Payroll UAT from an isolated worktree.
