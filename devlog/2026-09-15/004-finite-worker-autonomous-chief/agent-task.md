# Finite Worker Autonomous Chief

Implement the requested Ralph V3 transition from long-lived Native Goal workers to finite worker turns, with External-first Chief transport and Host Codex Sol fallback, richer Chief reasoning plus strict machine footer, next_worker_task support, isolated Ralph-owned worktrees, deterministic tests, and real Payroll UAT. Preserve V3 state semantics, Machine Gate, liveness compatibility, GUI fallback, and safety boundaries. Persist all required durable evidence before invocations.
