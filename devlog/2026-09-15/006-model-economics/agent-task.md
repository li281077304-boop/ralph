Implement the Ralph model-economics hard constraint on this branch. Default every ordinary development and Worker invocation explicitly to gpt-5.6-luna. Keep External Chief as the primary decision route and Host Sol High only for Chief SELECT/REVIEW/RECOVERY/FINAL_REVIEW fallback or explicit escalation; never make Sol a long-running Worker. Persist a usage ledger entry for every Worker, Chief, External Chief, and direct codex-task invocation with role/provider/model/reasoning/phase/run/round/duration/token fields (null plus an explicit unavailable marker when token data is absent), fallback reason, and failure signature. Ensure direct scripts/codex-task supports explicit role/model overrides while defaulting to Luna and keeps permissions independent from Devlog persistence. Add focused regression tests for defaults, fallback accounting, next-worker Luna routing, and ledger records. Run focused and full tests, typecheck, build, formatting, and diff checks. Do not run long Payroll UAT or change Payroll/Dashboard/Ralph state-machine semantics.
