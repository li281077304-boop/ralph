CONFIRMED CONCLUSIONS

- Payroll had no canonical Ralph project state or obligation ledger in the checkout; the bootstrap imported one obligation from existing README/CURRENT_STATE/PAYROLL_CORE/ASSET_INDEX evidence without inventing business rules.
- Finite Codex Worker execution must not use the Docker-backed `runStage` default on this host. The production Codex worker path now uses the installed host Codex CLI.
- Review cleanliness ignores Git porcelain's leading status column correctly while preserving all non-devlog changes.
- The isolated run completed the available orchestration path (SELECT → Worker → Machine Gate → checkpoint → Chief Review) and stopped only when the Chief identified absent authorized real Payroll inputs/UI verification prerequisites.

REJECTED ASSUMPTIONS

- A repository checkout is not evidence that its sensitive real Excel payroll package is present.
- A successful checkpoint or sanitized fixture is not real Payroll UAT PASS.

OPEN RISKS

- The generic V3 work loop still represents a true source prerequisite as `HUMAN_REQUIRED` while the imported obligation remains `TECHNICAL_OPEN`; the full obligation scheduler/Human Backlog semantics are outside this minimal bootstrap task.
- External GUI Chief was attempted once and failed because no GUI bridge configuration was supplied; Host Sol fallback handled the route.
