Close the three permanent Ralph Core architecture gaps, using the already-finished Dashboard run (`dashboard-widget-real-20260916-r2`, DONE/done/round 5, HEAD `a115f1b`) as real accident/regression evidence only. Do not continue that run, do not fabricate a continuation, do not re-engineer the Dashboard Widget.

P0 — Machine Gate must fail closed. A required gate command that exits non-zero, times out, never executed, or produced missing/malformed evidence must fail the gate: no checkpoint, no commit, no push, no CHIEF_PASS, no phase advance. It must route to a technical recovery/blocked state with the obligation still TECHNICAL_OPEN, and the next Worker attempt must re-run the gate. `policy_passed = true` must never override a required command failure. Cover the six regression cases and run a real safe UAT that deliberately fails one required gate command, then repairs it and re-gates to PASS.

P1 — The supervisor must not restart legal wait states. The observed `restart_count = 231` spin was entirely `WAITING_FOR_CHIEF`/waiting and is a real defect. Define the permanent restart matrix: never auto-restart DONE, WAITING_FOR_HUMAN, a legal WAITING_FOR_CHIEF with no pending autonomous recovery, CONTROLLED_PAUSE, USER_STOP, or an intentional exit. Restart only on unexpected crash/abnormal exit/disappearance when the durable ledger still has RUNNABLE or TECHNICAL_OPEN work. Add bounded backoff, rapid-restart detection, and a durable restart reason/counter/fingerprint; a crash loop is a technical incident, not a human business question. Run supervisor UATs A–E.

P2 — Make execution-backend failover permanent. Add a formal Execution Backend Router/Adapter contract separate from the Chief Router: the Controller keeps run state, ledger, handoffs, hashes, validation, the writer lock, the gate, checkpoints and transitions; a backend only executes and returns a raw result and can never mutate authoritative state. Support HOST_CODEX and EXTERNAL_AGENT (not WorkBuddy). Classify failures as QUOTA_EXHAUSTED, BACKEND_UNAVAILABLE, TRANSPORT_FAILURE, PROCESS_CRASH, AUTH_FAILURE, EXECUTION_FAILURE; the first three must fail over to the next configured backend without failing the run. Never duplicate execution: consult durable state and the gate verdict before resuming or switching. Use the anonymized Dashboard-run fixture as real evidence.

Also separate the layers explicitly (Supervisor / Controller / Chief Router / Execution Backend Router / Durable State), update the verified capability registry with DETERMINISTIC_TESTED / REAL_UAT_VERIFIED / USER_OBSERVED / NOT_YET_VERIFIED statuses, and run focused tests, the full suite, typecheck, build, formatting and diff checks.
