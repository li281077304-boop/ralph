Make the existing V3 supervisor the default public Ralph launch path without changing state-machine semantics. Keep the Controller as an internal/debug entrypoint, add a real default-entry regression/UAT that kills the Controller and proves automatic restart/resume, and preserve explicit stop/terminal/restart contracts. Then use the existing Ralph-owned Dashboard worktree and current DASHBOARD_OBLIGATIONS.json to run the real Android Widget technical obligation through External Chief → finite Luna Worker → Machine Gate → checkpoint → Review, with one Controller kill/restart during the run. Do not modify Payroll or WorkBuddy, do not re-bootstrap Dashboard, do not redesign Human Boundary, do not auto-merge or push production. Persist route, phase, worker, gate, checkpoint, restart, and widget evidence. Run focused/full tests, typecheck, build, formatting, and diff checks.
